
&НаКлиенте
Процедура ОбработатьНовыеВерсии(Команда)
	Если НЕ ЗначениеЗаполнено(Объект.ОписаниеХранилища) Тогда
		Сообщить("Не заполнено описание хранилища");
	КонецЕсли;
	
	ОбработатьНовыеВерсииСервер();
КонецПроцедуры                    

&НаСервере 
Процедура ОбработатьНовыеВерсииСервер() 
	
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.ОбработатьНовыеВерсии();  
	ЗначениеВРеквизитФормы(ОбработкаОбъект, "Объект");
	
КонецПроцедуры   

 